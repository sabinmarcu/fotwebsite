> .container > section
        position fixed
        top 25px
        bottom 25px
        left asidewidth + 50
        right 25px
        transform translateY(0)
        transition opacity 0.5s ease-in-out
        opacity 0
        text-align center
        overflow-y auto
        overflow-x visible
        & > *
            text-align left
        &.active
            transform none
            opacity 1
            & ~ section
                transform translateY(200%)
        nav.phonemenu
            display none