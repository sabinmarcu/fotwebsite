// Generated by CoffeeScript 1.7.1
(function() {
  var IS, chai, obj, _count;

  IS = require("./../lib/isf.js");

  chai = require("chai");

  obj = IS.Variable;

  chai.should();

  _count = function(json) {
    var key, nr, value;
    nr = 0;
    for (key in json) {
      value = json[key];
      nr++;
    }
    return nr;
  };

  describe("Variable Object", function() {
    it("Should spawn properly", function() {
      var n, x;
      n = obj.clone();
      n.should.equal(n);
      x = n.spawn();
      return x.should.be.an["instanceof"](n);
    });
    return it("Should have different values for different variables", function() {
      var n, y, z;
      n = obj.clone();
      z = n.spawn();
      y = n.spawn();
      y.set(true);
      z.set(false);
      (y.get()).should.not.equal(z.get());
      (y.get()).should.equal(true);
      return (z.get()).should.equal(false);
    });
  });

}).call(this);
